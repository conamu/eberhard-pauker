build-job:
  stage: build
  script:
    - echo "Building Docker Image"
    - docker build --tag "conamu470/eberhard-pauker" .

deploy-prod:
  stage: push
  script:
    - echo "Logging in to Dockerhub..."
    - docker login -p v5i7aLP6spvvbzc2 -u conamu470
    - echo "Pushing Image to Dockerhub!"
    - docker push build --tag "conamu470/eberhard-pauker"
